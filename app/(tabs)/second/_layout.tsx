import { View, Text } from "react-native";
import React from "react";
import { Stack } from "expo-router";

type Props = {};

const LayoutSecond = (props: Props) => {
  return <Stack />;
};

export default LayoutSecond;
